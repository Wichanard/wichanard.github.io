# --- THEME CORE ---
remote_theme: mmistakes/minimal-mistakes
minimal_mistakes_skin: "neon"

# --- RGB ROBOT INJECTION (ใส่ Markdown/HTML ในนี้เพื่อเปลี่ยนทั้งหน้า) ---
title: "WICHANARD // UNIT-01"
name: "Wichanard Kumsang"
description: >-
  <style>
    body { background-color: #000 !important; cursor: crosshair; }
    /* สร้างไฟ RGB วิ่งรอบหน้าจอ */
    .masthead, .page__footer { 
      border-top: 3px solid; border-bottom: 3px solid;
      border-image: linear-gradient(90deg, #ff0000, #00ff00, #0000ff, #ff0000) 1;
      animation: rgb-move 3s linear infinite;
    }
    @keyframes rgb-move { 0% { filter: hue-rotate(0deg); } 100% { filter: hue-rotate(360deg); } }
    
    /* ปรับแต่ง Sidebar ให้ดูเป็นหุ่นยนต์ */
    .sidebar { 
      background: rgba(0, 255, 0, 0.05); 
      border: 1px solid #0f0; 
      padding: 10px; 
      border-radius: 10px;
      box-shadow: 0 0 10px #0f05;
    }
    .author__name, .author__bio { font-family: 'Courier New', monospace !important; color: #0f0 !important; }
  </style>
  <div style="color: #0f0; font-family: monospace;">[SYSTEM ONLINE: OPTIMIZING AI NODE]</div>

# --- AUTHOR SETTINGS ---
author:
  name   : "WICHANARD KUMSANG"
  avatar : "/PIC/1.jpg"
  bio    : "🤖 AI STUDENT & DEVELOPER<br><span style='color:#0f0; text-shadow: 0 0 5px #0f0;'>● RGB PROTOCOL ACTIVE</span>"
  location: "THAILAND NODE"
  links:
    - label: "DECODE GITHUB"
      icon: "fab fa-fw fa-github"
      url: "https://github.com/Wichanard"

# --- LAYOUT SETTINGS ---
defaults:
  - scope:
      path: ""
      type: pages
    values:
      layout: single
      classes: wide
      author_profile: true
      header:
        overlay_image: https://image.lexica.art/full_jpg/09a0b12e-848e-4340-b63e-10821c326d9c
        overlay_filter: 0.5

# --- ENGINE ---
markdown: kramdown
highlighter: rouge
